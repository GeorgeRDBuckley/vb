<%- title "Which of these beers are vegan?" %>
<div class="main-container">
  <%= render 'layouts/searchbar' %>

  <section class="space--xxs">
                <div class="container">
                    <div class="row">
                        <div class="col-md-12">
                            <h1>Beers</h1>
                            <ol class="breadcrumbs">
                                <li><a href="#">Home</a></li>
                                <li>Beers</li>
                            </ol></div>
                    </div>
                </div>
            </section>

            <section class="bg--secondary space--xxs">
                <div class="container">
                <%= paginate(@beers) %>
                    <div class="masonry">
                        <div class="masonry__container row masonry--active">
                        <% cache @beers do %>
                                <%= render partial: 'beer-index', collection: @beers, cached: true %>
                                <% end %>
                                <% end %>
                        </div>
                    </div>
                    <%= paginate(@beers) %>
                </div>
            </section>

            